// Remember, we're gonna use strict mode in all scripts now!
'use strict';

// prettier
//It is an opinionated(strong opinin ) code formatter,that means it make assumutions how our code should look like.
// we need to define prettier as the default code formatter
// we need enable formatOnsave to formate when we save the code.

const x = 22;
if (x === 22) console.log(23);

//

const calAge = birthYear => 2023 - birthYear;
console.log();
